core {
    coursera.API = "INSERT_API_KEY_HERE"
    course.session = "vlsicad-001"
}

kbdd {
    // Assumes that kbdd can be found in a the bin directory RELATIVE to the execution path
    location = "./bin/kbdd"
}